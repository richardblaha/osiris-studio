apiVersion: v1
kind: Service
metadata:
  name: {{ include "osiris-web.fullname" . }}
  labels:
    {{- include "osiris-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "osiris-web.selectorLabels" . | nindent 4 }}
