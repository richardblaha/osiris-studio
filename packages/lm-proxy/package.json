{
  "name": "@osiris-studio/lm-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "An OpenAI-compatible HTTP shim over an editor Language Model API (VS Code / Copilot Chat), so a container-side agent gets its models from the editor.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@richardblaha/shared-core": "^0.1.0"
  },
  "devDependencies": {
    "@richardblaha/agent-core": "^0.1.0",
    "@osiris-studio/tsconfig": "workspace:*",
    "@types/node": "^22.7.5",
    "eslint": "^9.12.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
